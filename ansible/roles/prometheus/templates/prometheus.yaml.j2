global:
  scrape_interval: "15s"
scrape_configs:
  - job_name: node
    static_configs:
{% for target in prometheus.targets %}
      - targets: 
        - {{ target.ip }}
        labels:
          instance: {{ target.label }}
{% endfor %}
